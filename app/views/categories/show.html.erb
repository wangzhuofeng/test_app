<h1 align="center">Category <%= @category.name.capitalize %></h1>
<% if logged_in? and current_user.admin? %>
  <div class="center row" style="margin:10px;"><%= link_to ("<span class='glyphicon glyphicon-pencil' aria-hidden='true'></span> Edit").html_safe, edit_category_path(@category), class: "btn btn-xs btn-primary" %></div>
<% end %>

<div class="center"><%= will_paginate @category_articles %></div>
<%= render 'articles/listarticle', obj: @category_articles %>
<div class="center"><%= will_paginate @category_articles %></div>
